# 1. 忽略所有文件 (Ignore everything)
*

# 2. 但是！不要忽略文件夹 (Keep directories)
!*/

# 3. 但是！不要忽略任何带后缀的文件 (Keep files with extensions)
!*.*

# 4. (重要) 手动“放行”那些必须提交、但没有后缀的特殊文件
!gitignore
!Makefile
!Dockerfile
!LICENSE
!README

# VSCode extension build artifacts
node_modules/
target/
out/
*.vsix
