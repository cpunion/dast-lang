struct Point {
    x: int,
    y: int,
}

fn main() {
    let mut p = Point { x: 1, y: 2 };
    let a = p.x;
    p.y = 3;
    let pr = &mut p.y;
    *pr = 4;

    let mut arr = [1, 2, 3];
    let b = arr[1];
    arr[2] = 5;
    let ar = &mut arr[0];
    *ar = 6;

    println("sum", a + b);
}
