struct Point {
    x: i32,
    y: i32,
}

enum OptionI32 {
    Some(i32),
    None,
}

fn main() {
    let p = Point { x: 1, y: 2 }
    let Point { x, y } = p
    println("x", x)
    println("y", y)

    let opt = OptionI32.Some(5)
    let .Some(v) = opt
    println("v", v)
}
