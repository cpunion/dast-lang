fn assert_true(cond: bool, msg: String) {
    if !cond {
        println("assert_true failed", msg)
        exit(1)
    }
}

fn test_util() {
    assert_true(util_value() == 42, "expected util_value == 42")
}
