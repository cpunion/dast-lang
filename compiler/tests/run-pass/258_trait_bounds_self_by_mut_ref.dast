trait Bumper {
    fn bump(self: &mut Self) -> i32;
}

struct Point {
    x: i32,
}

impl Bumper for Point {
    fn bump(self: &mut Self) -> i32 {
        self.x = self.x + 1
        return self.x
    }
}

fn main() {
    let mut p = Point{ x: 2 }
    let v = p.bump()
    println("v", v)
}
