struct Box[T] {
    value: T,
}

impl[T] Box[T] {
    fn get(self: &Self) -> T {
        return self.value
    }
}

fn identity[T](x: T) -> T { x }

fn main() {
    let b = Box[i32]{ value: 42 }
    let x = identity(7)
    println("x", x)
    let r = &b
    let y = r.get()
    println("y", y)
}
