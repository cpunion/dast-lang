fn add(x: &i64, y: &i64) -> i64 {
    *x + *y
}

fn inc(x: &mut i64) {
    *x = *x + 1
}

fn main() {
    let mut v = 1
    let mut w = 2
    let sum = add(&v, &w)
    println("sum", sum)
    inc(&mut v)
    println("v", v)
}
