import "std/testing" as t

fn test_main() {
    let a: i32 = 'a'
    let n = '\n'
    let q = '\''
    let b = '\\'
    t.assert_eq!(a, 97)
    t.assert_eq!(n, 10)
    t.assert_eq!(q, 39)
    t.assert_eq!(b, 92)
}
