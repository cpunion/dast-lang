import "std/testing" as t

fn test_main() {
    let a: char = 'a'
    let n = '\n'
    let q = '\''
    let b = '\\'
    t.assert_eq!(a, 'a')
    t.assert_eq!(n, '\n')
    t.assert_eq!(q, '\'')
    t.assert_eq!(b, '\\')
}
