enum Maybe {
    None,
    Some(int),
}

import "std/testing" as t

fn test_main() {
    let m = Maybe.Some(7)
    let mut v = 0
    if let .Some(x) = m {
        v = x + 1
    } else {
        v = 0
    }
    t.assert_eq!(v, 8)
}
