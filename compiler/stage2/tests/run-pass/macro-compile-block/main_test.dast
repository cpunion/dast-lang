macro fn mk_block(x: AstExpr) -> AstBlock {
    quote block { let t = $x; t + 1 }
}

import "std/testing" as t

fn test_main() {
    let v = compile!(mk_block!(quote expr { 5 }))
    t.assert_eq!(v, 6)
}
