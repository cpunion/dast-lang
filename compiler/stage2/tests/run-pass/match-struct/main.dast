struct Point { x: int, y: int }

fn score(p: Point) -> int {
    match p {
        Point { x: 0, y: 0 } => 0,
        Point { x, y } => x + y,
    }
}

fn main() {
    println("a", score(Point { x: 0, y: 0 }))
    println("b", score(Point { x: 2, y: 3 }))
}
