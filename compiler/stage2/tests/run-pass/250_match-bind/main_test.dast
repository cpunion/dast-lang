import "std/testing" as t

fn test_main() {
    let n = 10
    let mut big = false
    match n {
        x if x > 5 => {
            big = true
            t.assert_eq!(x, 10)
        },
        _ => { t.assert!(false) },
    }
    t.assert!(big)
}
