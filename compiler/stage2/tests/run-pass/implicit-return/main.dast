fn add(a: int, b: int) -> int { a + b }

fn abs(x: int) -> int {
    if x >= 0 { x } else { 0 - x }
}

fn last_expr(x: int) -> int {
    let v = x + 1
    v * 2
}

fn main() {
    println("add", add(2, 3))
    println("abs", abs(-5))
    println("last", last_expr(4))
}
