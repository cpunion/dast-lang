// Test closure syntax - basic parsing

fn main() {
    // Empty closure works
    let f = || 42
    println("closure", 1)

    // Note: Closures with params currently have scope issues
    // let add = |a: i32, b: i32| a + b  // TODO: fix param scope
}
