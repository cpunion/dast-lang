// Test closure with typed params

fn main() {
    // Empty closure
    let f = || 42
    println("empty_closure", 1)

    // Closure with typed params
    let add = |a: i32, b: i32| a + b
    println("typed_closure", 2)
}
