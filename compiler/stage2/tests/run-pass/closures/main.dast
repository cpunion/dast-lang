// Test closure calling

fn main() {
    // Empty closure
    let f = || 42
    let result1 = f()
    println("empty_closure_result", result1)

    // Closure with params
    let add = |a: i32, b: i32| a + b
    let result2 = add(10, 20)
    println("add_result", result2)
}
