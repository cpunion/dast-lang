// Test mutable capture (FnMut semantics)

import "std/testing" as t

fn test_main() {
    // Test 1: Simple mutable counter
    let mut count = 0
    let incr = || { count = count + 1 }
    incr()
    incr()
    incr()
    t.assert_eq!(count, 3)
}
