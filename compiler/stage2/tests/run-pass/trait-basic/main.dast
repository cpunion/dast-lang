// Test trait definition and implementation parsing

trait Greeter {
    fn greet(&self) -> String;
}

struct Person {
    name: String,
}

impl Greeter for Person {
    fn greet(&self) -> String {
        return "Hello, " + self.name
    }
}

fn main() {
    println("trait impl parsing test passed")
}
