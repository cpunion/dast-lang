import { add } from "math"
import "math" as m
import { Point } from "geom"
import "std/testing" as t

fn test_main() {
    let v = add(2, 3)
    let w = m.mul(3, 4)
    let p = Point { x: v, y: w }
    t.assert_eq!(v, 5)
    t.assert_eq!(w, 12)
    t.assert_eq!(p.x, 5)
    t.assert_eq!(p.y, 12)
}
